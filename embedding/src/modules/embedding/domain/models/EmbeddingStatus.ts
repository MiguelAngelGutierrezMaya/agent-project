/**
 * EmbeddingStatus
 *
 * @description
 * Enum representing the possible statuses for embedding processing.
 */
export enum EmbeddingStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
