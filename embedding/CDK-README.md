# Embedding Service CDK

This project contains the CDK infrastructure for the Embedding microservice.

## Commands

- `pnpm cdk:deploy:dev` - Deploy to development environment
- `pnpm cdk:deploy:prod` - Deploy to production environment
- `pnpm cdk:destroy:dev` - Destroy development environment
- `pnpm cdk:destroy:prod` - Destroy production environment
