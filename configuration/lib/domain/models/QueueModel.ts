import { StackModel } from './StackModel';

/**
 * Queue model interface
 *
 * @description Defines the contract for queue stack implementations.
 * This interface extends StackModel to provide queue-specific functionality.
 *
 * @interface QueueModel
 * @extends {StackModel}
 */
export interface QueueModel extends StackModel {}
